<% provide(:title, "Home") %>
<div class="jumobtron">
	<h1>Bricked Electronics Shop</h1>
	<h2>Not your average Lego&trade; kit</h2>
</div>

<div class="container-fluid"></div>
	
	<% product = Product.all.sample(1) %>
	 
	 <div class="row productLine">
	 	<div class="col">
	 		<!--
	 		<%#= image_tag(product.image, :alt => "Image for #{product.title}", :width => "400px", :class => "center-block") %>
			<h4 class="title center"><%#= product.title %></h4>
			<h6 class="price center"><%#= number_to_currency(product.price) %></h6>
			<%# if logged_in? %>
			<div class="container">
			<button class="add_to_cart_btn" onclick="">
			  <%#= link_to "Add to Cart", add_to_cart_path(:u_id => current_user.id, :p_id => product.id ) %>
			  <h2>Add to Cart&nbsp;<span class="glyphicon glyphicon-shopping-cart"></h2></span>
			</button>
			<h2 class="review">
			  <%#= link_to "write review", new_review_path(:u_id => current_user.id, :p_id => product.id) %>
			</h2>
			</div>
			<%# end %>
	 		-->
	 		<%= render product %>
	 	</div>
	</div>
	<div class="row reviewLine">
		<%= render product.reviews.sample(1) %>
	</div>
</div>