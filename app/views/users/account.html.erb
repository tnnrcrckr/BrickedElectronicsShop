
<% provide(:title, "Account") %>

<div>
    <p class="storeTitle">Account Page</p>
</div>

<div class=container>
<%=
    #Disabled until Sessions are made
    #if(current_User.nil?)
        render "users/form"
    #else
        #render "users/manage"
    #end
%>
</div>
