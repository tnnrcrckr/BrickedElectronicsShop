
<% provide(:title, "Account") %>

<div>
    <p class="storeTitle">Account Page</p>
</div>

<div class=container>
<%=
    if(logged_in?)
        redirect_to users_path
    else
        render "users/form"
    end
%>
</div>
